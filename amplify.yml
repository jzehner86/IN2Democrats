version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p dist
        - cp -r css js images *.html favicon.svg dist/
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: []
